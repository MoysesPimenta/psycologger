{
  "buildCommand": "prisma generate && next build",
  "framework": "nextjs",
  "regions": ["gru1"],
  "crons": [
    {
      "path": "/api/v1/cron/payment-reminders",
      "schedule": "0 12 * * *"
    },
    {
      "path": "/api/v1/cron/appointment-reminders",
      "schedule": "0 11 * * *"
    }
  ],
  "_comment_oneoff_crons": "Backfill endpoints (NOT scheduled — run manually after deploy): POST /api/v1/cron/encrypt-cpfs and POST /api/v1/cron/encrypt-clinical-notes with header `Authorization: Bearer $CRON_SECRET`."
}
