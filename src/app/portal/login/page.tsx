import { PortalLoginClient } from "@/components/portal/portal-login-client";

export const metadata = {
  title: "Login — Portal do Paciente",
};

export default function PortalLoginPage() {
  return <PortalLoginClient />;
}
