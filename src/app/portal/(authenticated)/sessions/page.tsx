import { PortalSessionsClient } from "@/components/portal/portal-sessions-client";

export const metadata = { title: "Sessões — Portal do Paciente" };

export default function PortalSessionsPage() {
  return <PortalSessionsClient />;
}
