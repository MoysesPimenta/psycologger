import { PortalJournalNewClient } from "@/components/portal/portal-journal-new-client";

export const metadata = { title: "Nova Entrada — Portal do Paciente" };

export default function PortalJournalNewPage() {
  return <PortalJournalNewClient />;
}
