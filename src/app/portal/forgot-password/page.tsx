import { PortalForgotPasswordClient } from "@/components/portal/portal-forgot-password-client";

export const metadata = { title: "Esqueceu a Senha — Portal do Paciente" };

export default function PortalForgotPasswordPage() {
  return <PortalForgotPasswordClient />;
}
