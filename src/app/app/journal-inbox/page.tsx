import { JournalInboxClient } from "@/components/journal/journal-inbox-client";

export const metadata = { title: "Diário dos Pacientes — Psycologger" };

export default function JournalInboxPage() {
  return <JournalInboxClient />;
}
